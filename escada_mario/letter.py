start = 1
base = 5
space = 1

def spaces(space: int) -> None:
    for _ in range(space):
        print(' ', end = '')

def print_character(character, end) -> None:
    for count in range(end):
        
        

def letter(base):
    inversed_count = base - 1
    loop = True

    while loop:
        spaces(inversed_count)
        print('*')

        # character

        # espaços entre character
        # character
        
        inversed_count -= 1
        break

letter(base)
